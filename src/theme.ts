import { createTheme } from "@mui/material";

const theme = createTheme({      
  typography: {
    button: {
      textTransform: 'none'
    }
  },
  palette: {
    //type: 'light'
  }
});

export default theme;